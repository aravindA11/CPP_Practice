Exercise 7.39: Would it be legal for both the constructor that takes a
string and the one that takes an istream& to have default
arguments? If not, why not?

Ans: No, we can't have 2 constructors with default initilisation. If it is so the compiler can't decide which function to call.