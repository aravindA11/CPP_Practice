Exercise 7.8: Why does read define its Sales_data parameter as a
plain reference and print define its parameter as a reference to
const?

Ans: read modifies both stream and the object. where as print doesn't modify the object so constant reference is being used.