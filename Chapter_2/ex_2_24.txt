 Exercise 2.24: Why is the initialization of p legal but that of lp illegal?
 int i = 42;    void *p = &i;     long *lp = &i;

 Ans: p is a void pointer. so, p can hold any type of object address.
 l is a pointer to long and i is an int object. so it is illegal.