Exercise 6.35: In the call to fact, why did we pass val - 1 rather
than val--?

Ans: if we pass val-- it decrements val and is used later in the same expression. 
The behavior is undefined as there is no guarentee which operand gets evaluated first.