Exercise 6.1: What is the difference between a parameter and an
argument?

Ans: Arguments are values passed to a function, parameters are variables which represent the passed values inside the function.