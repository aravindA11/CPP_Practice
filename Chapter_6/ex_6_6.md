Exercise 6.6: Explain the differences between a parameter, a local
variable, and a local static variable. Give an example of a function in
which each might be useful.

Ans: A parameter is a variable initialised during function call. local variable are variables defined inside the function.
static vairables are same as local variables but they exists even after the function has completed its execution.