Exercise 6.20: When should reference parameters be references to
const? What happens if we make a parameter a plain reference when it
could be a reference to const?

Ans: The paramteres can be reference to constant when the function will not change the value of reference.
We can also pass literals to these kind of parameters.

If the parameters are plain reference then we can modify the value but the arguments type should be a variable.