Exercise 9.30: What, if any, restrictions does using the version of
resize that takes a single argument place on the element type?

Ans: if the objects stored doesn't have a default constructor then the program doesn't compile.