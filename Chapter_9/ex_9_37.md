Exercise 9.37: Why don’t list or array have a capacity member?

ANs: list doesn't require memory allocation overhead. array have fixed size elements. so capacity is not required in both the containers.