Exercise 10.5: In the call to equal on rosters, what would happen if
 both rosters held C-style strings, rather than library strings

Ans: THen the pinter arithematic happens instead of == on characters.