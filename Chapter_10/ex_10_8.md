Exercise 10.8: We said that algorithms do not change the size of the
containers over which they operate. Why doesn’t the use of
back_inserter invalidate this claim?

ANs: back_inserter uses push_back to insert elements rather than trying to access the elements.